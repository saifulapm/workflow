{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "mem questions --json",
  "description": "Questions in scope. A filtered read that matches nothing prints this document with an empty array and exits 1 (spec §7): callers that treat empty as fine test the array, not the code.",
  "type": "object",
  "required": [
    "questions"
  ],
  "properties": {
    "questions": {
      "type": "array",
      "items": {
        "$ref": "item.json"
      }
    }
  },
  "additionalProperties": false
}
