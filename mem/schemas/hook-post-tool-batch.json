{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "mem context --brief --hook-json --session-id <id>",
  "description": "Emitted on every fifth batch and nothing on the others (spec §9). The runtime validates this envelope: an unknown key is dropped and a wrong hookEventName fails the whole output.",
  "type": "object",
  "required": [
    "hookSpecificOutput"
  ],
  "additionalProperties": false,
  "properties": {
    "hookSpecificOutput": {
      "type": "object",
      "required": [
        "hookEventName",
        "additionalContext"
      ],
      "additionalProperties": false,
      "properties": {
        "hookEventName": {
          "enum": [
            "PostToolBatch"
          ]
        },
        "additionalContext": {
          "type": "string"
        }
      }
    }
  }
}
