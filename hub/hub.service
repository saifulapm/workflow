[Unit]
Description=hub — mem's phone-facing view
After=network-online.target

[Service]
Type=simple
ExecStart=%h/.local/bin/hub
Environment=PATH=%h/.local/bin:%h/.cargo/bin:/usr/local/bin:/usr/bin
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
